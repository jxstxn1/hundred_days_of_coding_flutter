import 'package:args/command_runner.dart';

class HdcCommandRunner extends CommandRunner {
  HdcCommandRunner()
      : super('hdc', 'A convenience script that simplifies setting up and running the Network Health Frontend project.');
}
